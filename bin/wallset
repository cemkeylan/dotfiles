#!/bin/sh

# Dmenu Script to change wallpaper
export selection=$(ls $HOME/Pictures/Wallpapers | grep -i .jpg | dmenu)
if [$selection = ""]
then
	exit
else
	feh --bg-fill $HOME/Pictures/Wallpapers/$selection
fi
