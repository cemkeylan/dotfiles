#!/bin/sh

VID=$(ls $HOME/.watchlater | dmenu -p "What will you watch")
if [ "$VID" = "" ]
then
	exit
else
	mpv $HOME/.watchlater/"$VID" >/dev/null 2>&1 &
	notify-send -u low "MPV" "Now Watching:\n$VID"
fi
