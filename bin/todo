#!/bin/sh

if [ "$(pacman -T calcurse)" = "calcurse" ]
then
	if [ -s $HOME/todo.txt ] 
	then
		echo "$(head -n 1 $HOME/todo.txt) "
	else
		echo "Nothing to do!"
	fi
else
	calcurse -t | sed 1d - | head -n 1 - | cut -c 4-
fi
