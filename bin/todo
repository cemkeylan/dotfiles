#!/bin/sh

if ! [ "$(calcurse --version 2>/dev/null)" ]
then
	if [ -s $HOME/todo.txt ] 
	then
		echo "$(head -n 1 $HOME/todo.txt) "
	else
		echo "Nothing to do!"
	fi
else
	cat $HOME/.calcurse/todo | head -n 1 - | cut -c 5-
fi
